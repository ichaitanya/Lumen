#!/usr/bin/env node
// Simple deployment script for Cloudflare Pages
// Pages handles the actual deployment, we just need to exit successfully
console.log('✓ Build artifacts ready for deployment');
process.exit(0);
